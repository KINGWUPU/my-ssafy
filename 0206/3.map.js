// const arr = [1,2,3,4,5,6];
// // 원본 배열을 손상시키지 않는 선에서 
// // 새로운 배열을 생성한다
// const newArr = arr.map((data) => data+1);

// [도전]
const arr1 = [1,2,3,4,5];
const newArr1 = arr1.map((data) => data **2);

const arr2 = ["a", "bcd", "ef", "g"];
const newArr2 = arr2.map((data) => data.length);
